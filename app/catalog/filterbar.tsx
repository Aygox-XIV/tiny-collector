export interface CatalogFilterBarProps {}

export const CatalogFilterBar: React.FC<CatalogFilterBarProps> = ({}) => {
    // TODO: catalog view selection, license filter selection
    return <div className="catalog-filter">Catalog Filter</div>;
};
